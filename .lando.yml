name: opendialog-webchat
services:
  php-service:
    type: php:7.3
    via: cli
    xdebug: true
tooling:
  test:
    service: php-service
    description: Runs all tests
    cmd: php /app/vendor/bin/phpunit